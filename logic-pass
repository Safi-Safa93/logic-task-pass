1.

str = "Python"

print (str) 

res_str = str.replace('o', '') 

print (res_str)

2. 

min = int(input("Enter the min : "))
max = int(input("Enter the max : "))

for n in range(min,max + 1):
   if n > 1:
       for i in range(2,n):
           if (n % i) == 0:
               break
       else:
           print(n)

3. 

counter = test_str.count('e') 

print (str(counter))
